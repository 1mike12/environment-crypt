{
  "name": "environment-crypt",
  "version": "1.0.0",
  "description": "encrypt environment variables so that they can be checked into version control",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Qin",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.3",
    "mocha": "^8.3.0"
  },
  "dependencies": {
    "minimist": "^1.2.5"
  },
  "bin": {
    "secure_env_json": "cli.js"
  }
}
